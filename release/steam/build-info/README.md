# Build validation

See [the working document](../../../docs/STEAM_STORE_READY_REPORT.md). Final manual review/production is pending. No assets are automatically submitted.

`candidate.json` preserves the historical v0.1.9 Steam-readiness candidate and its measured hashes. It does not describe later GitHub release binaries. For a numbered release, use that release's Windows ZIP and SHA-256 file; rebuilding replaces the local `dist/windows` package.
