import { useGame } from '../../context/useGame';
import { exhibitionAchievements } from '../../game/tournamentRewards';
import { PlayerLink } from '../game/PlayerLink';
import { formatMoney } from '../../utils/formatters';
export function ExhibitionAchievements(){const {gameState}=useGame();const wins=exhibitionAchievements(gameState);return <section id="exhibition-achievements" className="card scroll-mt-4 p-4" aria-label="Exhibition achievements"><h2 className="font-semibold text-white">Exhibition achievements · {wins.length}</h2><p className="mt-1 text-xs text-gray-400">Your exhibition wins are celebrated here. They do not add competitive titles or world-ranking credit.</p><div className="mt-3 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{wins.map(w=><article key={w.id} className="rounded border border-amber-400/20 p-3"><h3 className="text-sm font-semibold text-white">{w.name}</h3><p className="mt-1 text-xs text-amber-300">Exhibition winner · {w.season}</p><p className="mt-2 text-xs text-gray-300">{w.date} · {formatMoney(w.prize)}</p>{w.opponent&&<p className="mt-1 text-xs text-gray-300">Final {w.score} vs <PlayerLink name={w.opponent}/></p>}</article>)}</div>{!wins.length&&<p className="mt-3 text-xs text-gray-500">No exhibition wins recorded yet.</p>}</section>}
